<ion-nav-bar class="bar-positive">
      <ion-nav-back-button>
      </ion-nav-back-button>
</ion-nav-bar>
<ion-view view-title="Register">
        <ion-content class="padding">
	  <div class="card" id="app-user-registration-address" ng-hide="userRegistrationS2">
          <div class="item item-divider">Registration - Contact Details</div>
      
        <div class="list">
	
		<label class="item item-input item-stacked-label">
			    <span class="input-label">Email</span>
			    <input type="text" placeholder="john@xyz.com" ng-model="user.email" id="{{ $storage.a }}" value="{{ $storage.a[1].email }}">
		</label>
		<label class="item item-input item-stacked-label">
			    <span class="input-label">Phone Number</span>
			    <input type="text" placeholder="504-881-5455" ng-model="user.phno" value="{{ }}">
		 </label>
		<label class="item item-input item-stacked-label">
		 <span class="input-label">Mailing Address</span>    	 
		 <textarea placeholder="1234 xyz street" ng-model="user.maddress" value="{{  }}"></textarea>
		 </label>
		 <label class="item item-input item-stacked-label">
		    <span class="input-label">District</span>
		    <input type="text" placeholder="Chitoor" ng-model="user.district" value="{{ }}">
		  </label>
		<label class="item item-input item-stacked-label">
		    <span class="input-label">City</span>
		    <input type="text" placeholder="Hyderabad" ng-model="user.city" value="{{  }}">
		 </label>
		<label class="item item-input item-stacked-label">
		    <span class="input-label">State</span>
		    <input type="text" placeholder="Andhra Pradesh" ng-model="user.state" value="{{   }}">
		 </label>
		<label class="item item-input item-stacked-label">
		    <span class="input-label">Country</span>
		    <input type="text" placeholder="India" disabled>
                    
		 </label>
		<input type="hidden" ng-model="user.pageNumber" value="2"/>
		<div class="item item-divider item-light">
	   		<button class="button button-block button-positive" ng-click="{{ saveandforward }}">{{ navButton }}</button>
			<button class="button button-block button-positive" ng-click="previous()">Prev</button>
		 </div>
          
	  </div>
      </div>
	  </ion-content>
</ion-view> 
